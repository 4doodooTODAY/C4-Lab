Drop licensed Horizon webfont files here as Horizon.woff2 (and Horizon.woff).
The @font-face in src/styles/tokens.css picks them up automatically.
Until then, display text falls back to Poppins — nothing breaks.
